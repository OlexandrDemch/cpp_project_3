#include <iostream>

int main() {
    int a, b, c, d, e, f, g;
    std::cout << "Enter 7 integers: ";
    std::cin >> a >> b >> c >> d >> e >> f >> g;

    int max = a;
    if (b > max) {
        max = b;
    }
    if (c > max) {
        max = c;
    }
    if (d > max) {
        max = d;
    }
    if (e > max) {
        max = e;
    }
    if (f > max) {
        max = f;
    }
    if (g > max) {
        max = g;
    }

    std::cout << "The maximum number is: " << max << std::endl;
    return 0;
}
